// Sample of Wrapper component but it is advisable to use <></> or <Fragment></Fragment> tag. Just remember to import react or fragment

const Wrapper = (props) => {
  return props.children;
};

export default Wrapper;
